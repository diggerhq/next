export enum runStageEnum {
  running = 'running',
  queued = 'queued',
  pending_approval = 'pending_approval',
  pending_apply = 'pending_apply',
  succeeded = 'succeeded',
  rejected = 'rejected',
  failed = 'failed',
}
